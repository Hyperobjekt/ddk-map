import React from 'react'
import clsx from 'clsx'

/**
 * Displays contents specific to a tract (for display in slideout
 * panel or modal dialog)
 */
const TractPanel = () => {
  return (
    <div className={clsx('tract-panel-parent')}>
      tract panel parent
    </div>
  )
}

export default TractPanel
